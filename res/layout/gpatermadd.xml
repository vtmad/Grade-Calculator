<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/maroon"
    android:gravity="center_horizontal"
    android:orientation="vertical" >
	<!-- android:gravity="center_horizontal" -->

	<LinearLayout
	    xmlns:android="http://schemas.android.com/apk/res/android"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:background="@color/maroon"
	    android:gravity="center_horizontal"
	    android:orientation="horizontal" 
	    android:paddingTop="30dp"
	    android:paddingBottom="30dp">

		<TextView
		    android:layout_width="150dp"
		    android:layout_height="wrap_content"
		    android:gravity="left"
		    android:text="Number of enrolled courses this term:" />
    <!-- android:onClick="startGPATerm" -->

		<Spinner
		    android:id="@+id/numberOfCourses"
		    android:layout_width="wrap_content"
		    android:layout_height="wrap_content"
		    android:entries="@array/courses_array"
		    android:layout_marginTop="10dp"
		    android:prompt="@string/SPNnumCourses"/>

    </LinearLayout>
    
    <Button
        android:id="@+id/startGPATermCalc"
        android:layout_width="250dip"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/proceed"
        android:gravity="center_horizontal" />

</LinearLayout>