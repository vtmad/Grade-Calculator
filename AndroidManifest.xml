<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.mad.app.hokiehelper"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name=".GPAHome"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GPATermAdd" />
        <activity android:name=".GPATermCalc" />
        <activity android:name=".GPATermFinal" />
        <activity android:name=".GPAOverAllAdd" />
        <activity android:name=".GPAOverCalc" />
        <activity android:name=".GPAOverFinal" />
    </application>

</manifest>